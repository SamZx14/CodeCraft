#include<stdio.h>

void  syntax_sprint() {
    system("cls");
    c1(6);
    animation("Starting Syntax Sprint...\n",2);
    // Add logic for syntax sprint here
    c1(13);
            animation("\nPress any key to return to the menu...\n",2);
        _getch();
        c1(7);
}